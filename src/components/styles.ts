'use client'

import styled from "styled-components";

export const Button = styled.button`
    display: flex;
    width: 150px;
    height: 40px;
    align-items: center;
    justify-content: center;
`